{
  "name": "Movies db",
  "description": "ASimple movies reviews db",
  "image": "heroku/python",
  "repository": "https://github.com/sratatata/netguru-movies",
  "keywords": ["python", "django" ],
  "env": {
    "SECRET_KEY": {
      "description": "The secret key for the Django application.",
      "generator": "secret"
    }
  },
  "environments": {
    "test": {
      "scripts": {
        "test-setup": "python manage.py collectstatic --noinput",
        "test": "python manage.py test"
      }
    }
  }
}